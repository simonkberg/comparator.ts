---
"comparator.ts": minor
---

Add `localeComparator(locales?, options?)`, a locale-aware string comparator backed by `Intl.Collator`. It replaces the locale-dependent behavior that `stringComparator` had in v1.
