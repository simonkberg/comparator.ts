[**comparator.ts**](index.md)

---

[comparator.ts](index.md) / NaturalOrder

# Interface: NaturalOrder()

Defined in: [index.ts:33](https://github.com/simonkberg/comparator.ts/blob/main/index.ts#L33)

The call signatures of [naturalOrder](Variable.naturalOrder.md). Each overload accepts one kind
of [Comparable](TypeAlias.Comparable.md) so that values of different kinds cannot be compared
by accident.

## Call Signature

```ts
NaturalOrder(a, b): number;
```

Defined in: [index.ts:34](https://github.com/simonkberg/comparator.ts/blob/main/index.ts#L34)

The call signatures of [naturalOrder](Variable.naturalOrder.md). Each overload accepts one kind
of [Comparable](TypeAlias.Comparable.md) so that values of different kinds cannot be compared
by accident.

### Parameters

| Parameter | Type                 |
| --------- | -------------------- |
| `a`       | `number` \| `bigint` |
| `b`       | `number` \| `bigint` |

### Returns

`number`

## Call Signature

```ts
NaturalOrder(a, b): number;
```

Defined in: [index.ts:35](https://github.com/simonkberg/comparator.ts/blob/main/index.ts#L35)

The call signatures of [naturalOrder](Variable.naturalOrder.md). Each overload accepts one kind
of [Comparable](TypeAlias.Comparable.md) so that values of different kinds cannot be compared
by accident.

### Parameters

| Parameter | Type     |
| --------- | -------- |
| `a`       | `string` |
| `b`       | `string` |

### Returns

`number`

## Call Signature

```ts
NaturalOrder(a, b): number;
```

Defined in: [index.ts:36](https://github.com/simonkberg/comparator.ts/blob/main/index.ts#L36)

The call signatures of [naturalOrder](Variable.naturalOrder.md). Each overload accepts one kind
of [Comparable](TypeAlias.Comparable.md) so that values of different kinds cannot be compared
by accident.

### Parameters

| Parameter | Type      |
| --------- | --------- |
| `a`       | `boolean` |
| `b`       | `boolean` |

### Returns

`number`

## Call Signature

```ts
NaturalOrder(a, b): number;
```

Defined in: [index.ts:37](https://github.com/simonkberg/comparator.ts/blob/main/index.ts#L37)

The call signatures of [naturalOrder](Variable.naturalOrder.md). Each overload accepts one kind
of [Comparable](TypeAlias.Comparable.md) so that values of different kinds cannot be compared
by accident.

### Parameters

| Parameter | Type   |
| --------- | ------ |
| `a`       | `Date` |
| `b`       | `Date` |

### Returns

`number`
